{
  "name": "griddyup",
  "version": "0.0.1",
  "description": "",
  "main": "dist/griddyup.css",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "parcel -p 8080 src/index.html",
    "build": "rimraf dist && parcel build src/index.html --no-content-hash --no-cache"
  },
  "keywords": [],
  "author": "Austin Gil <austin@stegosource.com>",
  "license": "ISC",
  "devDependencies": {
    "parcel-bundler": "^1.12.2",
    "precss": "^4.0.0",
    "rimraf": "^2.6.3",
    "stylelint": "^9.10.1"
  }
}
